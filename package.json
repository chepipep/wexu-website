{
  "name": "wexu-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build:canvas && vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
