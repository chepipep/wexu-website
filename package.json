{
  "name": "wexu-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:canvas": "node scripts/canvas-to-site.mjs",
    "dev": "npm run build:canvas && vite",
    "build": "npm run build:canvas && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
